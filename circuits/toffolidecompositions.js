function ToffoliDecompositions()
{
    /*
        A key-value store representing the decompositions
        the gates are strings conforming to the unscheduledgate format
    */
    this.decompositions = {};
}

/*
    There is a standard notation for the decomposition:
    a is the first control
    b is the second control
    c is the target
*/
ToffoliDecompositions.prototype.addDecomposition = function(name, arrDecomposition)
{
    /*
        strings
    */
}

ToffoliDecompositions.prototype.applyDecomposition = function(gatePl, decompName)
{
    gatePl.
}